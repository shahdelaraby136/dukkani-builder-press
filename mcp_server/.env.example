# The base URL must point to the authenticated Dukani/Frappe API for one tenant.
DUKANI_API_BASE_URL=https://example.dukani.ai
# Store this only in the deployment secret store. Never commit a real token.
DUKANI_API_TOKEN=replace-with-secret
